create_table("apps") {
    t.Column("id", "uuid", {primary: true})
    t.Column("name", "string", {})
    t.Column("hostname", "string", {"size": 50})
    t.Column("language", "string", {"size": 15})
    t.Column("code_repo", "string", {})
    t.Column("image_repo", "string", {})
}